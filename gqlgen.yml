# go-gql-server gqlgen.yml file
# Refer to https://gqlgen.com/config/
# for detailed .gqlgen.yml documentation.

schema:
  - internal/gql/schemas/schema.graphql
# Let gqlgen know where to put the generated server
exec:
  filename: internal/gql/generated/exec.go
  package: gql
# Let gqlgen know where to put the generated models (if any)
model:
  filename: internal/gql/generated/models/models.go
  package: models
# Let gqlgen know where to put the generated resolvers
resolver:
  filename: internal/gql/generated/resolvers/resolvers.go
  type: Resolver
  package: resolvers
autobind: []
